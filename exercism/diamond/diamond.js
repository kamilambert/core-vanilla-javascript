var Diamond = function() {};

Diamond.prototype.makeDiamond = function(letter) {
  // create variable to contain new array
  // if first variable = variable.length = 1, then return variable
  // if variable.length
  // figure out distance letter being passed is from beginning of alpha
  //double the letter being passed

  if (letter === 'A') {
    return 'A\n'
  } else if (letter > A){
      for 
  }

}

module.exports = Diamond;
